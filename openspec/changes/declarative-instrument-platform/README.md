# declarative-instrument-platform

Define the declarative modular instrument platform around the existing YAML patch graph, inline `module_definitions`, primitive/composite/script/preset decision framework, validation/diagnostics, staged acceptance examples, and future tooling readiness.

LLM-assisted authoring is an intended future consumer of the platform, not an implementation target of this change.