---
description: Route the next OpenSpec task to the required model tier
---

Use the `openspec-route-task` skill to inspect and route the next pending task for this change:

`$ARGUMENTS`

Do not implement during routing unless the user explicitly asks to continue and the skill confirms that the current
model satisfies the task's minimum tier.
